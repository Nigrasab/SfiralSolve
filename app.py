############################
# app.py
############################
import streamlit as st
import numpy as np
import matplotlib.pyplot as plt

# Из Qiskit берём инструменты для создания и симуляции квантовой схемы
from qiskit import QuantumCircuit, Aer, transpile
from qiskit.visualization import plot_histogram

############################
# НАСТРОЙКА СТРАНИЦЫ
############################
st.set_page_config(
    page_title="Sfiral Interference MVP",
    layout="centered",
    page_icon="🌀"
)

############################
# ВВЕРХУ — ЛОГО/ЗАГОЛОВОК
############################
st.title("Сфиральная квантовая схема")
st.markdown("""
**MVP-приложение** для генерации и визуализации _«Сфиральной»_ квантовой схемы, 
демонстрирующей интерференцию во времени и устойчивые узлы вероятности.

> _Как это связано с биологией, временем и сознанием?_
> 
> - **Временная интерференция**: фрактальные фазовые сдвиги напоминают ритмы в биосистемах.  
> - **Устойчивые узлы**: самые вероятные состояния похожи на «привычки» или «стабильные паттерны» мышления.  
> - **S-образная топология**: имитирует спиральное переплетение, 
  отражающее цикличность восприятия и биологических ритмов.
""")

############################
# ПАРАМЕТРЫ ПОЛЬЗОВАТЕЛЯ
############################

# Количество кубитов
n_qubits = st.slider("Число кубитов:", min_value=2, max_value=8, value=5)

# Количество шотов (измерений)
shots = st.slider("Число измерений (shots):", min_value=100, max_value=4096, value=1024, step=100)

# Кнопка генерации
if st.button("Сгенерировать и запустить схему"):

    # Создаём квантовую схему
    qc = QuantumCircuit(n_qubits, n_qubits)

    # 1) Суперпозиция
    for i in range(n_qubits):
        qc.h(i)

    # 2) Фрактальные фазовые сдвиги (сфиральная логика)
    #    phi_i = pi / (3 + 2*i)
    import math
    for i in range(n_qubits):
        qc.rz(math.pi / (3 + 2*i), i)

    # 3) S-образные CNOT-связи (обратные)
    for i in range(n_qubits // 2):
        qc.cx(i, n_qubits - i - 1)

    # 4) Повторная суперпозиция
    for i in range(n_qubits):
        qc.h(i)

    # 5) Измерение
    qc.measure(range(n_qubits), range(n_qubits))

    ######################################
    # ВИЗУАЛИЗАЦИЯ СХЕМЫ
    ######################################
    st.markdown("### Сгенерированная квантовая схема:")
    # Отображаем схему через qiskit
    # plot_circuit = qc.draw(output="mpl")  # plot_circuit — рисунок
    fig_circ = qc.draw(output="mpl")
    st.pyplot(fig_circ)

    ######################################
    # ЗАПУСК СИМУЛЯЦИИ
    ######################################
    simulator = Aer.get_backend("qasm_simulator")
    compiled_qc = transpile(qc, simulator)
    result = simulator.run(compiled_qc, shots=shots).result()
    counts = result.get_counts()

    ######################################
    # ГИСТОГРАММА ВЕРОЯТНОСТЕЙ
    ######################################
    st.markdown(f"### Распределение вероятностей (шоты = {shots})")
    # Qiskit встроенная функция plot_histogram
    fig_counts = plot_histogram(counts, figsize=(10,5))
    st.pyplot(fig_counts)

    ######################################
    # ВЫДЕЛЯЕМ САМЫЕ ВЕРОЯТНЫЕ СОСТОЯНИЯ
    ######################################
    # Превращаем counts в сортированный список
    sorted_counts = sorted(counts.items(), key=lambda x: x[1], reverse=True)
    top_states = sorted_counts[:5]  # Топ-5 самых вероятных

    st.markdown("**Топ-5 устойчивых узлов (самые вероятные состояния):**")
    for state, freq in top_states:
        st.write(f"Состояние {state} — {freq} раз")

    ######################################
    # КОММЕНТАРИЙ К РЕЗУЛЬТАТАМ
    ######################################
    st.markdown("""
---
### Комментарий
- **Самое вероятное состояние** часто ассоциируется с "устойчивым узлом" в Сфиральной логике.
- **S-образные связи** задают волновую интерференцию, отражающую возможные резонансы в системе.
- **Связь с биологией**: повторяющиеся паттерны могут моделировать ритмы мозга или биосистем.
- **Связь со временем**: фазовые сдвиги напоминают фрактальные циклы, формирующие устойчивые точки восприятия.
""")

else:
    st.info("Нажмите 'Сгенерировать и запустить схему', чтобы увидеть результаты.")

############################
# Конец файла
############################
